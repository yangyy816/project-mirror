# P2-M5-R16 Q01 Runtime and Morphology Authority Repair

## Status and bounded authority

- `BOOTSTRAP_STATUS: OK`
- `TASK_ID: P2-M5-R16`
- `TASK_NAME: CC04-B Q01 Runtime Qualification and Morphology Admission Authority Repair`
- `FAILED_CANDIDATE_SHA: b501eb30f9520fc4f4345acd43ef4c4c372958b4`
- `FAILED_CANDIDATE_RUN: 32625820597`
- `FAILED_CANDIDATE_GATE_EVIDENCE: SAME_SHA_CI_PASS;EIGHT_ARTIFACTS_PASS;SECURITY_PRIVACY_RESEARCH_PASS;SOL_HIGH_FAIL`
- `REPAIR_CANDIDATE: THIS_COMMIT`
- `AUTHORITY_CONDITION: EFFECTIVE_ONLY_AFTER_THIS_COMMIT_SAME_SHA_CI_ARTIFACT_SECURITY_PRIVACY_RESEARCH_SOL_AND_PRINCIPAL_ACCEPTANCE`
- `PRE_CONDITION_CURRENT_STATE: CC04_B_Q01=FAILED_PENDING_FORWARD_AUTHORITY_REPAIR; CC04_B_EXECUTION=CLOSED`

The failed Q01 candidate remains immutable Git history. This forward Repair neither erases its successful Gates nor converts its Sol High failure into PASS. It repairs only the two cited authority gaps: fresh 04-B runtime qualification and deterministic morphology-cell admission. It creates no runtime, manifest, GenerationSpecification, Prompt, private capability, output, Asset, identity, cohort, or execution authority.

## Preserved Q01 decisions

All non-conflicting Q01 decisions remain unchanged: the specification and QA-overlay versions; raw and normalized SHA-256 exact-duplicate hard rejection; candidate-only pHash evidence before 04-C threshold freeze; one counting identity per confirmed duplicate cluster; fresh identity, Asset, calibration, holdout, split, and legacy isolation; six morphology and six nonsexual style cells with three-to-six cluster-adjusted admissions each; no downstream-performance selection; repaired R15/ADR-030 adult policy; and all custody, quota, holdout, 04-C through 04-E, MVR, M6, production, real-user, and P2-M7 closures.

## Fresh 04-B runtime qualification Gate

Q01's reference to the M3 normalization and admission baseline is evidence provenance, not 04-B scope approval. P2-M3 remains `LEGACY_STRICT_QUALIFICATION` and `EXCEEDS_CURRENT_RESEARCH_MINIMUM`, but its approval scope does not transfer to 04-B.

Before an E01 execution contract can be accepted, a separate tracked and independently accepted task must complete:

- `TASK_ID: CC04-B-V01`
- `TASK_NAME: 04-B Admission Runtime Qualification Review`
- `QUALIFICATION_TIER: CANDIDATE`
- `CURRENT_STATUS: NOT_QUALIFIED_FOR_04_B_PENDING_CC04_B_V01`
- `APPROVED_SCOPE: NONE_FOR_04_B`
- `PROHIBITED_SCOPE: 04_B_E01_USE;04_C_TRANSFORM_OR_DIAGNOSTIC_USE;HOLDOUT;PRODUCTION;DISTRIBUTION;REAL_USER_PROCESSING`

V01 may reuse tracked or recoverable M3 evidence, but it must make a fresh 04-B-scope disposition and may not inherit M3 identity, Asset, output, measurement, coverage, result, or approval authority. Its exact candidate must record all four ADR-043 fields and a digest-bearing manifest that proves:

1. exact first-party algorithm source revision and content digest;
2. exact runtime, toolchain, dependency closure, model or bundle digest, and origin;
3. code, model, weight, and data-license separation plus current license and vulnerability disposition;
4. private synthetic-only input scope, no credentials, no real-user data, and no public or production endpoint;
5. bounded or zero-network execution, explicit telemetry disposition, and no hidden download or substitution;
6. exact supported platform evidence, same-platform reproducibility, and every stricter already-frozen cross-platform requirement;
7. deterministic normalization, face-count, pose, landmark, reliability, and measurement behavior with versioned negative controls;
8. bounded resource, retry, recovery, integration, fail-closed, private SBOM, Security, Privacy, License, same-SHA CI, artifact, Sol High, and Principal evidence.

Only an independently accepted V01 result with `QUALIFICATION_TIER: APPROVED_FOR_INTERNAL_ENGINE`, `CURRENT_STATUS: APPROVED_FOR_INTERNAL_ENGINE`, and an `APPROVED_SCOPE` limited to the exact private synthetic 04-B admission runtime can satisfy this Gate. Any missing, mismatched, unavailable, network-active, telemetry-unknown, license-blocked, platform-unproven, or nondeterministic evidence returns `FURTHER_RESEARCH`, `BLOCKED`, or `FAILED`; it cannot be replaced by an install, download, new dependency, new model, new Provider, inherited approval, or verbal explanation.

## Deterministic morphology admission authority

The six Q01 morphology cells remain preregistered lower and upper bands for `UPPER_FACE_GEOMETRY`, `MIDFACE_GEOMETRY`, and `LOWER_FACE_GEOMETRY`. Human categorical judgment is not authority for band membership.

Before the first E01 call, the accepted V01 evidence and the later accepted E01 contract must bind one canonical morphology-measurement manifest with an exact version and SHA-256. For every band it must freeze the non-sensitive descriptor name, landmark inputs, formula, normalization reference, units, inclusive or exclusive bounds, missingness rule, reliability threshold, runtime and model digests, platform scope, repeat rule, and failure disposition. The manifest may measure admission suitability only; it may not choose 04-C formulas, transforms, tolerances, duplicate thresholds, READY dimensions, MVR results, or target ideals.

Admission must use the bound deterministic measurement. A human reviewer may decide only the already-allowed categorical presentation controls such as text or watermark absence, plain background, expression, visibility or occlusion, and nonsexual style-context match. A human reviewer must not assign, repair, override, or infer a morphology band.

Fail-closed outcomes are exact:

- missing or unaccepted manifest before the first call: `BLOCKED_MORPHOLOGY_MEASUREMENT_AUTHORITY_MISSING`;
- runtime, model, digest, platform, or formula mismatch before use: `BLOCKED_ALGORITHM_RUNTIME_AUTHORITY_MISMATCH`;
- unavailable or unreliable measurement for a returned output: `LANDMARK_OR_MEASUREMENT_RELIABILITY_FAILED`, with that raw output still counted and no retry;
- deterministic measured band not matching the immutable request assignment: `MORPHOLOGY_ASSIGNMENT_MISMATCH`, with no relabeling or human override;
- missing review or uncertain evidence: `UNSUPPORTED_PASS_OR_MISSING_EVIDENCE`.

No morphology count may contribute to the three-to-six occupancy bounds until the exact measurement evidence passes. Style-context review cannot substitute for morphology evidence, and continuous descriptor proximity cannot establish identity, sensitive traits, age, beauty, or a hidden standard face.

## Sequencing and closed boundaries

The repaired forward sequence is:

1. accept R16 and thereby close Q01 only after every R16 Gate passes;
2. create and independently accept `CC04-B-O01` without generation;
3. create and independently accept `CC04-B-V01` without generation or private-input discovery;
4. create and independently accept the separate `CC-P2-M5-04-B-E01` execution contract;
5. only then may the first generation call or private capability creation occur.

V01 is an additional evidence dependency already required by `CC04-A-05`; it does not replace or weaken the five-review T01 DAG. O01 remains the next task. Until O01, V01, and E01 are each independently accepted, `CC04_B_EXECUTION` remains closed.

This Repair does not change the 32 calibration raw / 24 independent admitted target, separate 32/24 holdout, 64 total output, concurrency-one, zero-retry, 768-transform, 2500-Vision-or-measurement, or 8-GiB ceilings. It does not create or access holdout, execute Vision, install or approve a dependency/model/runtime, modify schema/API/Worker/workflow, open 04-C through 04-E, evaluate MVR, open M6, or touch P2-M7 or shared-summary files.

## Validation and result

Acceptance requires an exact three-path Markdown allowlist; scoped Prettier and `git diff --check`; failed-candidate evidence preservation; ADR-043 field and V01 Gate scans; morphology manifest, deterministic measurement, no-human-substitution, and fail-closed scans; resource/adult/custody/holdout/downstream invariant scans; true-EOF last-occurrence, sentinel, and canonical/mirror equality; exact-SHA CI and all eight artifacts; independent Security/Privacy/Research Integrity and Sol High reviews; and Principal acceptance.

- `P2_M5_R16_RESULT: PASS`
- `CC04_B_Q01_RESULT: PASS_AFTER_R16_ONLY`
- `CC04_B_V01: NOT_STARTED`
- `CC04_B_EXECUTION: CLOSED`
- `NEXT_ACTION: CC04-B-O01`

These result markers are conditional and become current only when this exact commit satisfies every listed Gate. After acceptance, stop R16 and open only O01; do not create V01, E01, a private capability, or a generation call in the same candidate.
