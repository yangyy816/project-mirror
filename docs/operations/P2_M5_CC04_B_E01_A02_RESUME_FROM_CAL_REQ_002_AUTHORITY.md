# CC04-B-E01-A02 — Resume Authority from CAL-REQ-002

- `BOOTSTRAP_STATUS: OK`
- `TASK_ID: CC04-B-E01-A02`
- `OWNER_DECISION_ID: OD-P2-M5-CC04-B-E01-R29-001`
- `PREDECESSOR_ACCEPTED_AUTHORITY: e181452eae860a736237fa78420a6c5667579e56`
- `PREDECESSOR_CI_RUN: 32748331998`
- `A02_AUTHORITY_CONDITION: EFFECTIVE_ONLY_AFTER_THIS_COMMIT_SAME_SHA_CI_ALL_EIGHT_ARTIFACT_CONTENT_CHECKS_SECURITY_PRIVACY_LICENSE_RESEARCH_SOL_AND_PRINCIPAL_ACCEPTANCE`

## Purpose and strict boundary

A02 is a separate prospective execution checkpoint. It may authorize only `CAL-REQ-002` as the next never-used
formal ordinal after every stated Gate passes. It does not accept or retry failed `CAL-REQ-001`, create a cohort,
open 04-C/04-D/04-E, evaluate MVR, enter M6, create a QuestionBank entry, or authorize production/real-user work.

The inherited per-output order is mandatory: dispatch consumes the ordinal; exact native handle; create-new exact-byte
staging copy; SHA-256/byte size; non-decoding magic/media classification; immutable registry commit; valid committed
receipt; only then decode/dimensions, normalization, deterministic QA, pHash candidates, independent lightweight Sol
group screening, and provisional admission/rejection. Before the receipt, all decoding, preview/thumbnail, image
metadata, Pillow/OpenCV/browser parsing, dimensions/EXIF, landmarks, face count, pHash, normalization, QA, screening,
and admission are prohibited. Any registration failure is `OUTPUT_REGISTRATION_FAILED_BEFORE_DECODE`; the consumed
ordinal is not retried or followed. A repeated `DECODE_BEFORE_REGISTRATION` for `CAL-REQ-002` is
`REPEATED_OUTPUT_REGISTRATION_ORDER_VIOLATION` and requires Principal mechanism review, not an ordinary Repair.

## Preserved incident and current accounting

```text
CAL_REQ_001: CONSUMED_FAILED_NO_RETRY
CAL_REQ_001_FINAL_DISPOSITION: FAILED_NON_ADMISSIBLE_NO_RETRY
CAL_REQ_001_SOURCE_COPY: ABSENT_VERIFIED
CAL_REQ_001_STAGING_COPY: ABSENT_VERIFIED
CAL_REQ_001_PLATFORM_TRANSCRIPT_COPY: EXISTS_OR_UNKNOWN_NOT_UNDER_PROJECT_REGISTRY_DELETION_NOT_VERIFIED
FORMAL_E01_GENERATION_CALLS_EXECUTED: 1
FORMAL_E01_RAW_OUTPUTS_CREATED: 1
FORMAL_E01_PROVISIONAL_ACCEPTED_IDENTITIES: 0
FORMAL_CALLS_REMAINING: 31
FORMAL_RAW_OUTPUT_CAPACITY_REMAINING: 31
GLOBAL_NATIVE_OUTPUT_CAPACITY_REMAINING: 62
NEXT_FORMAL_ORDINAL: CAL-REQ-002
```

`CAL-REQ-001` may not be regenerated with the same or a changed Prompt, replaced, used in calibration/holdout/
QuestionBank, admitted, or refunded. No Prompt plaintext, private locator, private path, image bytes, URL, or credential
may enter tracked content, CI, MEMORY, or review material.

## First-wave presentation context and v2 specification

`FIRST_WAVE_PRESENTATION_PRIORITY` is `EAST_ASIAN_PRESENTING_ADULT_SYNTHETIC_FACES` for the China-market-first,
invite-only Beta generation context. It is not a race, ethnicity, ancestry, nationality, user-routing, sensitive
identity, or real-person inference field. The only permitted persisted context label is
`EAST_ASIAN_PRESENTING_FIRST_WAVE`; `ETHNICITY: ASIAN` and equivalent classifications are prohibited.

From `CAL-REQ-002` through `CAL-REQ-032`, the private canonical specification must be the new append-only version
`p2-m5-cc04-b-calibration-generation-v2-east-asian-first-wave`. Before dispatch it must bind its exact private digest,
the parent v1 version, this Owner decision, the presentation-context change, unchanged adult/synthetic-only/safety and
resource boundaries, and the accepted morphology/style assignment manifest. It must record that v1 remains historical
for failed `CAL-REQ-001`; it may not silently overwrite v1 or expose Prompt plaintext.

The priority does not reduce continuous upper/mid/lower-face geometry, face-shape, eye/nose/lip proportion, or
non-sensitive style-context diversity. The existing six morphology and six style coverage cells remain binding;
lighting/background/expression are standardized; celebrity imitation, beauty ranking, hidden ideal faces, and
single-template convergence are prohibited.

## Execution, review, and downstream closure

After accepted A02, execution is serial (`concurrency: 1`), zero-retry, at most four calls per tranche, starting only
at `CAL-REQ-002`. Each Sol group contains at most four images and is preliminary advisory only; every kept output stays
`HUMAN_REVIEW_STATUS: PENDING_SECOND_ROUND`. No runtime QuestionBank generation occurs. Future candidate intent is
`EAST_ASIAN_PRESENTING_ADULT_SYNTHETIC_FIRST` for the first-wave China-market context, but any future entry remains
behind E01 admission, 04-C/04-D/04-E, M5 technical Gate, P2-MVR-v1, M6 eligibility, immutable Beta manifest, and
revocation boundaries. Release stays `INVITE_ONLY_BETA_PENDING_HUMAN_SECOND_ROUND`; public, general availability,
permanent, and production release are closed.

## Acceptance criteria

This candidate must be a normal forward child of accepted R31 with exactly these four documentation paths. It requires
scoped formatting, `git diff --check`, allowlist and canonical/mirror true-EOF/last-occurrence checks, no private
leakage, exact accounting and no-retry checks, exact-SHA CI, actual eight-artifact inspection, independent Security/
Privacy/License/Research Integrity and Sol High reviews, then Principal acceptance. No image generation is permitted
until every A02 Gate has passed.
